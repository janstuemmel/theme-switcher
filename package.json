{
  "name": "theme-switcher",
  "scripts": {
    "clean": "rm -rf web-ext-artifacts",
    "start": "web-ext run -s src",
    "lint": "web-ext lint -s src",
    "build": "web-ext build -s src",
    "bundle": "run-s clean lint build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "web-ext": "^7.1.0"
  }
}
