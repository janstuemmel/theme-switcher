{
  "name": "theme-switcher",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "web-ext run",
    "lint": "web-ext lint",
    "clean": "rm -rf web-ext-artifacts",
    "build": "npm run clean && npm run lint && web-ext build -i package*.json LICENSE README.md docs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "web-ext": "^7.1.0"
  }
}
