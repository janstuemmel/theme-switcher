{
  "name": "theme-switcher",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "web-ext run",
    "lint": "web-ext lint",
    "build": "npm run lint && web-ext build -i package*.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "web-ext": "^7.1.0"
  }
}
